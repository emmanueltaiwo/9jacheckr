# 9ja Checkr

Monorepo: **web** (Next.js), **server** (Express API + auth), **bot** (Telegram).

```bash
npm install
npm run dev -w web    # site + dashboard
npm run dev -w server # API
npm run dev -w bot    # Telegram
```

See each app’s `README.md` for env vars.
